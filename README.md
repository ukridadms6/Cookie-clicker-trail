# Cookie Clicker Trail

## Interactive sandbox

Run the local interactive sandbox with:

```bash
./start-sandbox.sh
```

Optional custom port:

```bash
./start-sandbox.sh 8080
```

Then open:

- `http://127.0.0.1:4173/`
- `http://127.0.0.1:4173/cookie-clicker.html`
